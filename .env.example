# Railway sets PORT automatically.
PORT=3000

# Supabase project settings.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Keep service-role keys out of browser code.
# Add this only for server-side admin tasks when needed.
SUPABASE_SERVICE_ROLE_KEY=
